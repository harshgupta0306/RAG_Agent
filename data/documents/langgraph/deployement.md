# LangGraph Deployment

A LangGraph application can be deployed as a backend service.

Production applications should use persistent state rather than relying
only on in-memory storage.

Applications can expose graph execution through APIs and support
streaming responses.

Production deployments should also consider authentication, secrets,
logging, monitoring, scaling, and persistent databases.